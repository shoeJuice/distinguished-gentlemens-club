import { Image, Flex, IconButton, Button, Box } from "@chakra-ui/react";

/**
 * Render a Logo component
 */
export const Logo = () => {
  return (
    <Box data-testid="logo">
      <Button variant="unstyled">Distinguished Gentlemen</Button>
    </Box>
  );
};
